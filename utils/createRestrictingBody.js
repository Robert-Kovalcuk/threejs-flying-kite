import * as CANNON from 'cannon';

function createRestrictingBody(mesh, mass = 0, restrictAxes = { x: false, y: false, z: false }) {
	// Add a Plane shape to act as a floor and prevent falling through


	return body;
}

export default createRestrictingBody;
